server {
    listen 80 default;
    server_name saja-api;

    location = /favicon.ico { access_log off; log_not_found off; }

    location / {
        include         uwsgi_params;
        uwsgi_pass      unix:/<path-to-project>/serverconfig/uwsgi.sock;
    }
}
